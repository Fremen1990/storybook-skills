import {ArgsTable, Meta, Story, Canvas} from "@storybook/addon-docs";
import {Button, Props} from "../src/Button"

<Meta title="MDX/Button" component={Button}/>

export const Template = args => <Button {...args} children="Iam in MDX!!"/>;

# Button

With MDX/Button we can define a story for Button right in the middle of our markdown documentation.

<Canvas>
    <Story name="default button">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story={"default button"}/>

I want extra text description here!!!

<Canvas>
    <Story name="Secondary" >
        <Button variant="secondary" children="Secondary"></Button>
    </Story>
</Canvas>